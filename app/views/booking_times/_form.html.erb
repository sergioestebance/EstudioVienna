<%= form_for(@booking_time) do |f| %>
  <% if @booking_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking_time.errors.count, "error") %> prohibited this booking_time from being saved:</h2>

      <ul>
      <% @booking_time.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :booking_id %><br />
    <%= f.number_field :booking_id %>
  </div>
  <div class="field">
    <%= f.label :hora_id %><br />
    <%= f.text_field :hora_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
